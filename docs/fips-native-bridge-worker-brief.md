Implement a complete origin-preserving WMapp Tower FIPS bridge in /Users/mini/code/wm/wmapp. Manager authorized write scope via task comment 2026-09-09T06:03:38Z on task b5f96311-d672-46ca-ab2d-e0bb0cdde8f8, workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9. Read repo AGENTS and /Users/mini/code/wm/flightdeck/docs/fips-delivery-handoff-2026-09-09.md. No other worker owns WMapp; preserve all existing concurrent state. Tower worker owns /wm/tower; primary worker owns /wm/flightdeck; no edits there except communicate contract by callback/task comment.
Concrete bridge contract: inject window.wingmanTowerTransport = {version:1, connect: async ({endpoint, logicalTower}) => ({version:1, endpoint, proxyBaseUrl})}. endpoint exact validated http://<node npub>.fips:<port> origin; logicalTower current public HTTPS Tower origin only logical identity. Native connect requires explicit per-page-origin/manual Tower pairing approval, uses existing FIPS readiness. Return a loopback proxy http://127.0.0.1:<ephemeralPort>/<unguessable-route> (no trailing slash). Browser signs actual mesh endpoint+path+query, fetches proxyBaseUrl+path+query with same Authorization/body. Worker EventSource uses proxy route with token query. Proxy must stream SSE/binary/upload/download and cancellation/status without buffering whole SSE. Exact pinned FIPS endpoint only; no arbitrary open proxy, SSRF, redirects to public or other targets, cookie forwarding, key handling/signing, discovery. Strip hop-by-hop, forwarded-host/proto, Origin and cookies to Tower; set fixed exact mesh Host. Permit only approved requesting Flight Deck origin by CORS with preflight and local/private-network request header if platform requires; no wildcard origins, unguessable route and Origin checks. Bind IPv4 loopback only. CORS expose required content headers. Do not log route capability or SSE tokens. Failure propagates; never public fallback. Existing HTTPS/iPhone behavior unchanged. Do not weaken global WebView mixed content/TLS policy.
Crucial validate that existing platform WebView can fetch http loopback from HTTPS (loopback is potentially trustworthy, but platform restrictions differ). Prefer this existing native HTTP proxy route since it supports ordinary fetch and worker EventSource without moving sync ownership. If platform cannot use loopback without weakening global security, report immediately with concrete alternative native JS transport so primary can adapt. Integration tests should exercise real proxy sockets, origin/target restrictions, request/body/status, SSE stream/cancellation, failure and forbidden redirects, not only mocks. Document setup/endpoints/activation and any platform/browser pass still needed. No restart/stop Autopilot. Manager owns Tower runtime activation. Routine WMapp build authorized; coordinate runtime activation if needed via task comments. No raw keys: broker-only CLI auth. Primary worker owns task state/handoff (leave in_progress). Do not post chat. Commit tested compatible WMapp source with Conventional Commits after full worktree inspection; no push/stash/reset. Set session goal reflect, then stop after callback. Return changed files, tests/results, commits, exact runtime activation/validation remaining and endpoint usage. Task CLI can use explicit --tower-url https://sb4.otherstuff.studio --app-npub npub1hd37reqgfcnz3pvzj4grknd2nkzc94p9ercmunrxx22razr2rfxsw6dns5 --workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9 through /wm/autopilot/clis/wingman.ts. Report contract quickly in docs/fips-tower-bridge.md inside WMapp.
