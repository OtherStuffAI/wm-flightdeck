# Compact mobile inbox

The mobile inbox spreads each item across too many lines. Combine context and time, keep the title prominent, retain useful previews, and remove the redundant mobile Open pill where the whole card already provides navigation. Preserve distinct, accessible read/done actions and unread cues.

Keep changes scoped to the inbox, preserve concurrent work, and use the configured development runtime. No deployment or adjacent-service changes are requested.

Validate at 375, 390 and 430px plus desktop, checking overflow, keyboard activation, and task/chat/document routes. Run required repository tests, public-source checks, release-note validation, build and distribution verification. Report measurements, commit and limitations to the supervising session, which maintains the originating task and conversation.

Workspace-specific routing and screenshot references remain in the private task and supervisor handoff.
