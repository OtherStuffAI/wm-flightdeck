Repair Flight Deck OTA publisher CI and prepare distribution of current tested frontend.

User: "Please ensure we are deploying the newly distributed build for remote updates and pushed to git for rebuilds". Current source main8207b06 build1876 (Inbox footer50 and all31 channel mapping verified); WMapp main e451e91 packages1876, both GitHub pushed. Manager created origin flightdeck-release at8207b06, enabled GitHub Pages workflow source, added exact flightdeck-release environment branch allowlist (main retained), and fast-forwarded/pushed deployed to8207b06, returned main. Existing new publisher run33953932527 now fails at deterministic build because FLIGHT_DECK_PG_APP_NPUB is absent. This is confirmed, not hypothetical. Read gh run view33953932527 --repo OtherStuffAI/wm-flightdeck --log-failed.

Repo /Users/mini/code/wm/flightdeck main. Read AGENTS, docs/ota-publishing.md and .github/workflows/publish-flightdeck-ota.yml. Small required fix: pass public repository Actions variable vars.FLIGHT_DECK_PG_APP_NPUB into deterministic build env; clear early validation helpful if missing. Manager will set repository variable to same existing public app namespace npub1hd37reqgfcnz3pvzj4grknd2nkzc94p9ercmunrxx22razr2rfxsw6dns5. No private key or secret. Document exact setup step so repeatable rebuilds work. Preserve immutable release archive/epoch numbering/permissions/existing protection. Check other deterministic workflow steps for obvious issues and run targeted package/deterministic tests before push. No unrelated runtime source changes. Follow repo release metadata instructions proportionately for CI-only change; do not silently mutate live dist into epoch build (use isolated git archive/temp build if validating deterministic release).

Work main, preserve concurrent/untracked operational docs (public-source107 prior findings unchanged); commit all compatible tested public source, no raw secrets, resets/rebase/force or policy bypass. Push origin main only. Manager owns release/deployed promotion, GitHub repo settings, publish run and final artifact verification. No service or client restart, no deployment branch push by worker.

Reporting task created below in workspace2e5caefd-dd65-45d2-b747-ee874e8e5fc9, features channel0617d526-88dc-4dc2-9876-08349ab60eca, scope76d518f7-c477-4374-bf74-5d36fda570ed, originating thread40aec5c2-581c-4938-97ac-1d224abb5c69. Same direct conversation latest publish instruction takes precedence over old no-deploy handoffs. Supervisor owns external task/thread. Set own goal+reflect; final callback commit/push, changedfiles, tests and any publisher blockers. This is bounded CI repair, not a full platform redesign.

Tracking task c3372f9c-be5f-407b-928c-35054467fe39.
