# Chat integrity implementation progress

Worker dispatch context is absent: Flight Deck task/comments/thread MCP report no pipeline/document/Agent Direct binding; CLI task show reports missing Tower URL. Dispatch reporting context has workspace/channel/thread only, no backend/app binding. Manager: please provide latest task/comments/origin thread as a local context file and post execution contract on task; worker continues local implementation. No keys or adjacent services accessed.

Both supplied screenshots inspected. They show duplicate-looking title-only Unknown author roots alongside actual source threads. Investigating canonical thread/source materialization ordering. Mention root cause confirmed in code: record-delta actor sidecars bypass directory hydration, while mentions consume workspace_members, populated by settings roster fetch.

Scope: Flight Deck only; no historical data repair, shared-contract change, restart or deployment. Next: failing realistic roster/ordering regressions, fixes, required validation and commit.

17:04 AWST: manager context read; server links for screenshot example are intact. Six new failing regressions reproduced: missing cold-chat roster refresh; late roster written into new workspace DB; thread-only delta duplicates typed/POST source with title-only row; late POST overwrites newer SSE revision; POST after thread-only delta leaves placeholder; canonical ID hydration closes optimistic thread. Initial fixes pass 216 message/delta tests and 175 channel/delta tests. Additional sparse-POST source mapping regression being covered; no architectural/cross-repo changes needed. Registered app found running on loopback port 41045. Manager retains task reporting and state.

Additional retry finding for manager (17:05 AWST): resendFailedMessage currently copies attachments, generates a NEW client_request_id and deletes the failed local row. If Tower accepted the original POST but response was lost, that resend can create a second canonical message/thread. Need verify Tower's existing client-request idempotency hash/signature behavior (does replay of identical body/thread/metadata/client ID with freshly generated author signature deduplicate?) before deciding exact retry transport. Please inspect existing server contract read-only and provide answer here/context; no backend changes requested. Local coverage/fixes continue.

17:08 AWST validation: full suite running with --maxWorkers=2 after two timeout-only failures under higher parallelism. check:public-source fails on many PRE-EXISTING tracked docs/handoffs plus existing operator-name/domain literals elsewhere. No source cleanup or checker weakening will be done. Supplied/new local handoffs are also prohibited publication paths under current checker; planning to leave these untracked as intentional manager evidence and commit compatible implementation/tests/release metadata, unless manager explicitly directs otherwise. No generated dist committed.

17:14 AWST: same-ID retry regression proved TWO server records before fix for both roots/replies after lost response; now passes, retaining original attachment IDs and original reply thread even if another thread is open. Full-suite first run: 3572 pass, 3 failures. Two Inbox history failures were caused by removing required cached history rows; fixed by retaining history metadata and suppressing title-only cards at presentation. The remaining 20,000-row worker timeout also reproduces unchanged at HEAD in a separate /tmp git archive (no worktree changes), so it is pre-existing/environmental. Final suite/build/browser smoke remain. New local-browser smoke uses registered loopback runtime only and seeded presentation data; does not claim authenticated live Tower send coverage.

17:18 AWST: final identity tests 222/222 pass, including history metadata across canonical updates and late-response failure after SSE. Latest full run overlapped that final history test addition and reported its pre-fix failure plus the independently reproduced baseline worker timeout; final untouched-source full rerun pending after browser/build. Build 1897 in progress. Public-source failures will be reported separately; no checker bypass. Handoff docs remain local/untracked to avoid introducing additional prohibited publication paths.

17:25 AWST: build 1898 (`20260907-0923-10-1898`) verified both dist/version.json and registered runtime http://127.0.0.1:41045/version.json. Browser smoke now PASSES Chromium and WebKit (one test each) against built runtime with all external traffic blocked. Browser caught final channel-visible filtering gap; canonical roster agents now populate workspaceAgentNpubs before groups hydrate. Added exact regression and rebuilt 1898. Live signed Tower send tests remain a manager/manual check; the browser smoke uses isolated seeded UI state, while unit/integration tests execute real Dexie and realistic canonical PG shapes. Full final suite running. Source/test/release metadata staged, handoff/context docs intentionally untracked because public-source policy rejects that path. No deployment or restart by worker.

## Terminal implementation handoff — 2026-09-07

Commit: ad63e1a7c9ddb49a6d87d17960835115d4f69def (`fix(chat): hydrate agent mentions and preserve message identity across sync`). Task remains in_progress for manager review. Manager-provided latest recoverable context reread; worker Flight Deck tools still have no binding, so manager owns authoritative task comment and origin-thread reporting.

Root causes/fixes:
- Record-delta actor sidecars did not hydrate workspace_members; chat never explicitly fetched that mention roster. Cold chat entry and mention opening now request/coalesce the roster; late responses are discarded after workspace changes, and Dexie member updates refresh an open menu. Empty PG roster cannot fall back to another workspace's cached agents.
- Actual channel-visible composers omitted canonical roster agents until an Agents group loaded. Agent-kind roster entries now join the existing universally mentionable workspace-agent set.
- Thread-only canonical/typed refreshes could insert title-only summary rows beside source messages received through another route. Plain placeholders reconcile with exact source links; required history metadata remains cached but is excluded from displayed roots. Bounded queries fetch exact source IDs when older than the root page.
- Canonical ID hydration could close an optimistic open thread and lose its draft context. Identity navigation now follows the canonical source and keeps the draft. Late POST acknowledgements cannot overwrite newer SSE revisions, and late failures cannot resurrect replaced local rows. Sparse successful create_thread responses map their returned message as the root.
- Failed-send retry generated a fresh request ID and copied attachments, duplicating a server-accepted command after a lost response. Retry now preserves request identity/body/attachments and original thread routing, even after reload with a different thread open. Author-signature generation is unchanged. No historical server data changed.

Files committed (20): .build-meta.json, release-notes.json; src/app.js, channels-manager.js, chat-message-manager.js, chat-order.js, chat-presentation-cache.js, db.js, pg-record-delta.js, pg-write-adapter.js, section-live-queries.js, workspace-manager.js; tests/channels-manager-mixin.test.js, channels-manager.test.js, chat-message-manager.test.js, chat-order.test.js, mention-channel.test.js, pg-record-delta.test.js, pg-write-adapter-message-signatures.test.js, e2e/chat-integrity.spec.cjs.

Final validation:
- bun run test -- --maxWorkers=2: 3581 passed / 3582; 260 passed test files / 261. Sole failure tests/pg-materialization-responsiveness.test.js:42, 20,000-row worker exceeds existing 10s timeout. Same timeout reproduced against untouched HEAD exported to an isolated /tmp archive, and with Node 24. No timeout/test weakening. Final log /tmp/chat-release-full.log; baseline /tmp/chat-baseline-performance.log.
- Final identity focused pass: 222/222. Channel-visible mention and release-note focused pass: 34/34; release-note tests also pass in final full suite (8/8). Earlier focused ordering/history pass: 240/240.
- Chromium + WebKit browser presentation smoke: 1 pass each, pinned Playwright browsers under /tmp/chat-playwright-browsers with healthy Node 24. Logs /tmp/chat-browser-1898-chromium.log and /tmp/chat-browser-1898-webkit.log. External network blocked. These use seeded UI state; authenticated live Tower sends were NOT browser-tested. Unit/integration regressions use real Dexie and canonical PG fixtures, including ordering/reload/lost-response replay.
- bun run build: passed. Final build 1898 / 20260907-0923-10-1898. dist/version.json and http://127.0.0.1:41045/version.json match.
- bun run verify:dist and git diff --check: passed. agents.md and claude.md remain identical.
- bun run check:public-source: 177 pre-existing findings; zero findings in committed files. Log /tmp/chat-public-source-final.log. Did not clean unrelated source or weaken checks.

Runtime: registered Flight Deck runtime serves build 1898 on loopback 41045. No restart performed by worker, no remote deployment, no push, no Autopilot/Tower changes. No remaining worker-owned Playwright/install processes. Generated dist stays ignored/uncommitted.

Dirty files: the four supplied/progress/context/retry-contract docs/handoffs/2026-09-07-chat-integrity*.md remain intentionally untracked. The publication checker explicitly prohibits docs/handoffs paths; preserve these manager coordination files rather than adding new publication-policy violations.

Manager/Pete smoke steps (test channel):
1. Reload, enter chat directly and type an agent name after @ without opening settings/Setup Groups. Check it appears when roster loads. Switch to a workspace without that agent; confirm no old agent remains selectable.
2. Start a thread; type a reply while root send is pending. The existing loading guard must preserve the draft until the canonical thread exists. Send it once, and verify one root/one reply with the correct author; no separate Unknown/title-only card.
3. Repeat with a delayed/lost response and Retry, including an attachment. Confirm one canonical message/thread and original attachment; reopen another thread before retrying a failed reply to confirm its original destination is preserved.
4. Refresh and reopen the thread; confirm replies/history remain attached, draft survives identity reconciliation, and reading older history does not jump due to incoming updates. Check the two screenshot threads no longer display phantom summary cards. Historical records need no repair for the server sample verified by manager.
