Fix workspace isolation after creating/selecting a new workspace in Flight Deck.

Pete reports: "When I create a new workspace I still see the previous synced data from the previous worksapce i.e. Pete worksapce here when I should be in Winn worksapce…"
Source: @[Report](mention:message:c720b31f-8b75-4205-87d1-fba884de7f49), @[Features](mention:channel:0617d526-88dc-4dc2-9876-08349ab60eca), thread e0c2ecff-a386-4530-a720-671a09f98708. Reporting workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9; scope 76d518f7-c477-4374-bf74-5d36fda570ed.
Screenshot storage://a7d6be6c-50cc-498b-bd56-38c9efe2f734 downloaded to /tmp/workspace-isolation.png. Read image. It shows workspace selector Winns / Winn family Wi..., while sidebar shows Wingman Suite, Other Stuff, PRT, Quick Apps and inbox/feed contain Pete workspace items. Greeting Pete Winn is current user and alone is not evidence of a bug. Confirmed visual mismatch, root cause unconfirmed.

Repo /Users/mini/code/wm/flightdeck only. Read AGENTS.md and relevant README/docs. Hypothesis: workspace creation/switch leaves old Dexie/rendered collections, sync cursors or late asynchronous callbacks active, or resolves distinct workspaces sharing owner/backend/app identity to same local partition. Investigate before changing. Preserve data; do not clear all databases as a workaround. Check new workspace activation and existing workspace switching and startup restore. Validate identity with workspace_id and backend, not labels alone.

Acceptance: after Pete -> new Winns transition only Winns data appears across sidebar, home/inbox/feed, tasks, chat, docs/files, agents and WApps as appropriate to their actual scope. Delayed responses from previous workspace must not populate current UI; switching back restores previous workspace correctly; same-owner workspaces remain distinct; empty workspace and reload paths covered. Add focused regressions that reproduce the confirmed cause. Do not assume all user-global surfaces should be blank.

Worker owns implementation. Default main, preserve concurrent work; inspect full worktree and commit all compatible nonignored tested state with Conventional Commit. Follow release notes/version rules, focused tests, bun run test, bun run check:public-source, bun run build, bun run verify:dist, git diff --check. Dist ignored, do not commit generated files. No standalone preview, no deployment/push requested. Routine configured app restart authorized if necessary; never restart Autopilot. Stay in repo; if architecture/shared contracts/cross-repo changes needed, report before proceeding and resolve latest architecture whiteboard per manager instructions.

Set worker session goal and next action reflect. Read latest task/comments on start and before completion using broker tools. Keep task progress/evidence durable; manager handles chat and final review state, worker must not post chat. If worker lacks task routing report evidence in callback for manager to post. Return diagnosis, changed files, tests results/failures, commit, build version, any manual/browser verification remaining. Stop next action once handoff delivered. Manager will supervise and review.

Tracking task: @[Workspace isolation](mention:task:abceba9e-d33b-4ad6-a857-bb69aadbeaef). Task ID abceba9e-d33b-4ad6-a857-bb69aadbeaef; use explicit workspace above for CLI reads. Manager session 97f2b395-6b40-44c3-b2f8-322853b3e516.

Manager review follow-up (12:27 UTC): specifically verify existing pre-fix database key migration does not strand pending edits when switching to UUID-qualified keys. Document old database retention/rehydration/migration and add appropriate regression. Worker runtime send was denied 403 admin-or-execution-delegation-required; this durable brief carries the review concern. No new Pete thread messages as of last check.

## Worker handoff — 2026-09-06

Implemented on main. Screenshot read: the workspace selector/content mismatch is confirmed; the screenshot alone does not prove the affected runtime UUIDs. Code regressions reproduce same-owner/service/app partition collisions because PG keys omitted workspace_id. A second confirmed fault called clearRuntimeData after opening the destination DB, deleting cached rows/outbox/cursors on switch. Delayed individual PG reads also lacked workspace checks before writes.

Changes: UUID-qualified canonical keys shared by descriptor and normalization paths; unambiguous legacy-key restore; distinct-workspace merge and owner-selection behavior; compatible discovery/self-index key lookup; retain destination DB on activation; reset workspace UI collections and member directory; activation-generation guards for local loads, subscriptions, settings, PG persistence; preserve transaction DB binding. Shared address book/current user remain global. Existing materializer worker disposal remains in place.

Validation: focused isolation/create/select/descriptor/release regressions pass (final focused run: 51 tests). Full default bun run test: 3469 passed, one 10-second worker benchmark timeout. Benchmark isolated: passed, 20,000 rows in 5104 ms, maximum caller loop delay 6.9 ms. Full bun run test --maxWorkers=2: 255 files / 3470 tests passed. Build 1887 / 20260906-1228-4-1887; verify:dist and both working/staged diff checks pass. Public-source check fails on 107 findings, all in pre-existing tracked docs/handoffs paths; no task source failures. All pre-existing untracked handoffs retained outside commit because that directory is prohibited by the public-source check. Generated dist remains ignored.

Migration limitation: old UUID-less databases are retained untouched. New canonical partitions hydrate afresh from Tower; potentially mixed legacy rows are not copied automatically. Unsynced data in an old ambiguous partition remains on disk but requires explicit workspace attribution/recovery. Manager must review this limitation before accepting release. No actual-account browser pass was performed; verify new workspace activation, same-owner switching back, empty workspace reload, inbox/feed/files/chat/tasks/agents/WApps and user-global identity in the configured runtime. No deployment, push, standalone preview, restart or adjacent-repo edit performed.

Broker routing unavailable both at start and handoff: flightdeck_context had no run context/workspace/task; task-comments tool returns no pipeline/document/Agent Direct context. Explicit-workspace CLI task show returns missing PG Tower URL. Manager must post this evidence and read latest task/comments. No chat posted and no task review-state change made. Session metadata set to goal/reflect during work; stop after handing off. Commit identifier is supplied by final callback.
