# Live sync regression investigation

Pete reports: "We have recently updated flight deck and tower to allow for better syncing performance on wmapp on my phone now it refuses to sync any channels." Latest steering: "There was a tower issue during the rebuild and I see some sync errors popping up on different web versions can you check that?"

Investigate client compatibility and real materialization of the current record-sync response. Primary repo /Users/mini/code/wm/flightdeck, main at c7e6064. WMapp /Users/mini/code/wm/wmapp packages FD1874 installed today. Tower /Users/mini/code/wm/tower at5c97746. Read applicable instructions and performance design/status docs. No architecture or shared contract change authorized at this stage.

Manager is independently checking live Tower APIs and DB logs. Confirmed: Tower migration failed through04:43:13UTC missing function flightdeck_pg_record_context(jsonb); current container started04:43:15UTC and src/schema/run-migrations.ts hash matches migration fix5c97746. Health local3100 and public https://sb4.otherstuff.studio OK. Authenticated legacy /sync returns31 channels. V1 /record-sync?protocol_version=1&limit=200 starts returning records and31 channels. Manager draining pages with mycode/check-record-sync-20260905.ts. Request without protocol_version correctly returns400 unsupported_protocol_version (probe mistake, not a confirmed client defect).

Goal: identify and reproduce any deterministic client defect explaining all channels failing despite server200, especially materializer, initial resource-view bootstrap, native WKWebView support, old/new cache compatibility, persisted cursors and reset recovery. Use bounded tests and read-only live queries if helpful, with broker helper /Users/mini/wingmen/wingman21/mycode/performance-pg-request.mjs. Workspace2e5caefd-dd65-45d2-b747-ee874e8e5fc9; public app identity is in helper. No active Flight Deck thread context in this manager turn; report only via supervised callback, no external messages or task writes.

Initially diagnosis only: return exact failing path, evidence, smallest proposed fix and validation. Do not change source until manager follow-up. May add local diagnostic files in ignored/temp or operational handoff directories. Do not clear cache, sign out, install WMapp, restart or deploy services, change DB state, or search/export raw keys. Normal read-sync server cursor bookkeeping is expected. Preserve all concurrent state; default main; operational handoffs remain untracked per public-source constraints. If later implementation is authorized, commit compatible tested nonignored public source state and preserve excluded operational files; no resets/stash/rebase/force pushes. Use capped test workers4.

Set own session goal via supported metadata if available; report blocker rather than key workaround. Deliver root cause or clear limits, reproduction command/results and recommended next step.

## Manager live results at 06:14 UTC

V1 snapshot drained53 pages then successful delta handover page54, all HTTP200. Counts:31 channels,3828 messages,683 threads,457 tasks,1959 task comments,74 docs. No repeated cursor or server errors. Authenticated GET resource-view-states?limit=1 also succeeded. Public OPTIONS with Origin http://localhost and authorization,x-flightdeck-pg-app-npub returned204 and correct allowed headers/origin. PostgreSQL catalog confirms flightdeck_pg_record_context exists. No Tower error/failed/exception/timeout lines in last20min runtime logs. User-account/browser recovery still unobserved; these requests use Rick broker identity.
