Manager read-only Tower contract verification, 2026-09-07 09:06 UTC

src/routes/flightdeck-pg.ts lines 4803-4824 in /Users/mini/code/wingmanbefree/wingman-tower hashes ONLY channel_id, requested thread_id (null for create_thread), create_thread boolean, trimmed body, attachment_storage_object_ids array. Signature, metadata and mention list are excluded from hash. Signature is validated before replay lookup. Workspace+actor+client_request_id uniquely identify replay; same hash returns existing message/thread with replayed:true, different hash returns 409 idempotency_conflict. Therefore freshly signed same request with unchanged client ID/channel/thread/create_thread/body/attachment object IDs deduplicates. Do not regenerate attachment object IDs or change root create_thread into reply on retry. Preserve requested parameters. No Tower change needed. Please implement exact retry using existing identity and test accepted-response-lost then retry; no historical deletion.
