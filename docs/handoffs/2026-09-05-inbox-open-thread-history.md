# Restore history when opening an Inbox chat

Pete reports: "When I open a chat thread from the deck inbox, there is no history to the chat?" Authoritative message @[report](mention:message:518a835a-e474-4bd7-a451-21dac1d7439c), features channel0617d526-88dc-4dc2-9876-08349ab60eca, thread40aec5c2-581c-4938-97ac-1d224abb5c69, workspace2e5caefd-dd65-45d2-b747-ee874e8e5fc9, scope76d518f7-c477-4374-bf74-5d36fda570ed. Rick supervises and posts task/thread evidence; worker reports through callback/local handoff, no duplicate chat posts.

Repo /Users/mini/code/wm/flightdeck. Current baseline inspect main; previous published fbd21e9 build1878, WMapp /Users/mini/code/wm/wmapp 3ad64f5. Default main, preserve concurrent work and existing private untracked handoffs. Commit all nonignored tested compatible public state; do not publish private handoffs/captured data into public Flight Deck. No resets/rebases/force pushes. Read local AGENTS/skills.

Confirmed manager inspection: src/autopilot-overview-manager.js openDeckThread seeds applyMessages from channel-filtered fileMessages (bounded Inbox projection), then opens thread. Previous browser probe explicitly stubbed navigation actions, so it did NOT validate opening a real conversation. Trace actual openAutopilotOverviewThread -> openDeckThread -> openThread, dedicated thread subscription/pagination, query parent/thread IDs, cached and missing-data TowerSyncService paths. Do not assume missing rows indicate lost data.

Implement actual history loading on open without relying on Inbox preview rows or selected-channel history window. Initial visible thread page must include real conversation/replies; bounded older paging must reach full older history. Keep all channel metadata independent, Inbox50 + bottom footer and All/Chat/Task/Docs/Files intact. Preserve TowerSyncService -> worker -> Dexie -> liveQuery, pending commands, cursors, archive/deletion authorization. No broad history scans or unbounded materialization to fill window; use existing indexes / bounded keysets. Ensure cached history renders and missing history requests through sync service as needed, stale async opens cannot cross conversations, edits/new replies update correct open thread. Stay FD/WMapp; only propose Tower/shared changes if evidence proves necessary and supervisor reviews.

Required verification: regression invoking real Inbox card action/open handler, actual canonical mapped persisted thread/message rows with more than Inbox source window and multiple threads. Verify recent replies and older page navigation, cache-upgrade/ownerless rows where relevant, cold or partially cached thread fetch, next/previous thread rapid switches, returning to Deck preserving filter/position, new reply/edit/delete subscription. Render the actual opened thread in WebKit, not a navigation stub or synthetic cards only. Existing private capture /tmp/fd1874-live-page-0.json through53 may be used locally. Existing verifier scripts in scripts/verify-inbox-activity-browser.mjs and source probe demonstrate offline routing; extend/use production handlers. No standalone preview server or managed runtime restart. Focused tests, full repository-required tests/build/checks once fixes stable. Existing public-source check107 private handoff findings must not expand; changed public files pass. Record exact limits honestly.

Build next real product build after inspecting current metadata. Update WMapp bundle via tools/update_flightdeck_bundle.sh --use-existing-dist, validate packaged worker/updater and proportional Flutter tests. Push both GitHub main branches. Supervisor will fast-forward deployed and flightdeck-release and verify stable feed archive after accepting callback (user remote update/Git publication authorization persists). Worker MUST NOT promote release branches or restart/install apps. No cache/auth wipe. Set session goal/reflect; final local handoff cause, changes, exact tests, commits, packaged build and limitations; stop after handoff. Task stays in_progress until supervisor review.

Tracking task 38d66e08-aceb-4e14-b3f6-e349cc007724.
