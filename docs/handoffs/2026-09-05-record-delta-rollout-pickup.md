# Flight Deck protocol rollback pickup

Implement a focused continuation of Pete's requested Tower/Flight Deck performance work in /Users/mini/code/wm/flightdeck on main. Checkpoint 03de674 is source-reviewed; task remains in_progress because rollout recovery is incomplete. Task a39adf78-ce5e-4a26-834f-6d45b0e358ad, workspace2e5caefd-dd65-45d2-b747-ee874e8e5fc9. Origin @[Pete request](mention:message:b33b7c9e-4d32-4575-b2e5-6324e8f2c2e7), channel0617d526-88dc-4dc2-9876-08349ab60eca, thread40aec5c2-581c-4938-97ac-1d224abb5c69. Manager owns thread/review state. Read current task/comments through broker helper /Users/mini/wingmen/wingman21/mycode/performance-pg-request.mjs GET /api/v4/flightdeck-pg/workspaces/<workspace>/tasks/<task>/comments; don't self-skip explicit manager delegation.

Bug: src/pg-read-hydrator.js syncTowerPgRecordWorkspace only falls back on404/406/501 if !state && !cursor. A client that already negotiated v1 will fail forever if Tower rolls back to a version without /record-sync despite legacy /sync still being available. The original brief requires compatible rollout/rollback without clearing caches/pending commands and distinct recoverable cursors. Current design doc only says initial fallback and does not provide usable recovery for persisted v1 state. See manager's final rollout task comment.

Fix safe unsupported-protocol fallback after persisted v1 use. Preserve canonical/local cache, pending commands, v1 cursor and generation, and use only independent legacy cursor on legacy endpoint. Never pass v1 cursor to legacy. Never fall back on403, reset-required409, malformed pages, generic400/500 or transient network failures. A later restored v1 endpoint must resume/recover its own cursor safely. Cover first snapshot, partial committed snapshot, ordinary delta and endpoint disappearance as relevant. Inspect forceSnapshot: it currently clears authoritative cache before probing endpoint; ensure rollback recovery does not require cache deletion and don't introduce unsafe reset races. Keep worker/TowerSyncService ownership and generation CAS.

Tests: supported v1 stored state -> unsupported endpoint -> legacy request using old cursor, preserving v1 state/pending/cache; restored v1 resumes own cursor;403 still purges/hides and throws with no legacy request; invalid pages never fallback. Use existing injected tests/fixtures and no Tower edits. Read docs/design/incremental-cache-and-bounded-views.md and current status handoff for architecture/validation evidence. Tower final contract+fixture is ../tower/docs/design/flightdeck-record-delta-v1.md at1ee972c. No external research, secrets, shared restart/deploy or standalone runtime. Isolated test/browser harness allowed.

Preserve concurrent work. Tracked main clean at03de674; three operational handoffs are intentionally untracked due existing public-source prohibition; don't delete/change them or add private operational notes to public source. This pickup adds one operational handoff; public tested source/design evidence must be committed. Do not weaken checker. Prior107 public-source findings are unchanged baseline. Conventional commit compatible tested state; no resets/stash/cleanup/history rewrite.

Run focused rollback/cursor tests, then bun run test --maxWorkers=4 (prior full3395 tests passed; unrestricted default worker responsiveness test times out under load). Required build/release notes metadata nextbuild1874, verify:dist and actual generated module-worker smoke scripts/verify-incremental-worker-browser.mjs; diff checks. Update public design rollout semantics and repo-local status with exact command/results/commit, and task comment. No need rerun unrelated large benchmarks for narrow orchestration-only change unless evidence shows query/materialisation changed. Leave task in_progress for manager acceptance. Final callback to manager; no independent Pete thread post. Set own goal and reflect while working, stop after complete handoff.
